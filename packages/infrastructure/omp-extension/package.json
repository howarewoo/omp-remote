{
  "name": "@omp-remote/omp-extension",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/extension.d.ts",
      "default": "./dist/extension.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && esbuild src/extension.ts --bundle --format=esm --platform=node --external:node:* --outfile=dist/extension.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run src --passWithNoTests",
    "install:user": "node dist/install.js"
  },
  "devDependencies": {
    "@omp-remote/protocol": "workspace:*",
    "@oh-my-pi/pi-coding-agent": "catalog:",
    "@types/node": "catalog:",
    "esbuild": "0.28.1",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "zod": "catalog:"
  }
}
