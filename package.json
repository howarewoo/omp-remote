{
  "name": "omp-remote",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "node scripts/dev.mjs",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "test": "node --test scripts/dev.test.mjs scripts/install-service.test.mjs scripts/setup.test.mjs && turbo run test",
    "typecheck": "turbo run typecheck",
    "install:service": "node scripts/install-service.mjs",
    "setup": "node scripts/setup.mjs",
    "setup:extension": "pnpm --filter @omp-remote/omp-extension install:user",
    "start": "pnpm --filter @omp-remote/daemon start",
    "tailscale:serve": "node scripts/tailscale-serve.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
